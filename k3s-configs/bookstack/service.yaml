apiVersion: v1
kind: Service
metadata:
  name: bookstack-service
spec:
  type: NodePort
  selector:
    app: bookstack
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30001
