# this will not be needed after using hostPort, created for testing purposes
apiVersion: v1
kind: Service
metadata:
  name: minecraft-service
spec:
  type: NodePort
  selector:
    app: minecraft
  ports:
    - protocol: "TCP"
      port: 25565
      targetPort: 25565
      nodePort: 30002 
